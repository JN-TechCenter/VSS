# VSS 项目全局配置文件
# ==========================================
# 使用 Nginx 反向代理，简化端口管理

# ===========================================
# 应用基本信息
# ===========================================
APP_NAME=VSS Vision Platform
APP_VERSION=1.0.0
APP_DESCRIPTION=VSS 视觉平台 - 企业级视觉处理解决方案
COMPANY_NAME=JN-TechCenter

# ===========================================
# 对外端口配置 (通过 Nginx 代理)
# ===========================================
# 主要服务端口
NGINX_PORT=80              # 主入口：前端 + API 代理
NGINX_DEV_TOOLS_PORT=8080  # 开发工具：邮件测试
NGINX_ADMIN_PORT=8081      # 管理工具：数据库管理

# ===========================================
# 内部服务端口 (容器间通信)
# ===========================================
BACKEND_INTERNAL_PORT=3002
FRONTEND_INTERNAL_PORT=3000

# 数据库内部端口
POSTGRES_INTERNAL_PORT=5432
REDIS_INTERNAL_PORT=6379

# 开发工具内部端口
MAILHOG_INTERNAL_PORT=8025
ADMINER_INTERNAL_PORT=8080

# ===========================================
# 数据库配置
# ===========================================
DB_TYPE=h2
DB_HOST=localhost
DB_NAME=vss_db
DB_USERNAME=vss_user
DB_PASSWORD=vss_password
DB_MAX_CONNECTIONS=20
DB_CONNECTION_TIMEOUT=30000

# H2 数据库配置
H2_CONSOLE_ENABLED=true
H2_CONSOLE_PATH=/h2-console

# ===========================================
# API 配置
# ===========================================
API_PREFIX=/api/v1
API_TIMEOUT=30000
API_RATE_LIMIT=100

# ===========================================
# 前端配置 (通过 Nginx 代理访问)
# ===========================================
VITE_API_BASE_URL=/api
VITE_WS_URL=ws://localhost/ws
VITE_APP_TITLE=${APP_NAME}
VITE_APP_VERSION=${APP_VERSION}
VITE_APP_DESCRIPTION=${APP_DESCRIPTION}

# ===========================================
# Docker 配置
# ===========================================
DOCKER_NETWORK=vss-network
DOCKER_REGISTRY=
DOCKER_IMAGE_TAG=latest
DOCKER_NETWORK=vss-network

# 开发环境特定配置
DEV_HOT_RELOAD=true
DEV_SOURCE_MAP=true
DEV_MOCK_API=false

# 生产环境特定配置
PROD_OPTIMIZE=true
PROD_GZIP=true
PROD_CACHE_CONTROL=public,max-age=31536000

# 安全配置
JWT_SECRET=your-super-secret-jwt-key-change-in-production
SESSION_SECRET=your-session-secret-key
CORS_ORIGIN=http://localhost:3000

# 日志配置
LOG_LEVEL=info
LOG_FILE_PATH=./logs

# 监控配置
HEALTH_CHECK_ENDPOINT=/health
METRICS_ENDPOINT=/metrics

# 邮件配置（如果需要）
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@vss-platform.com
